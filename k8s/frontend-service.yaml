apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: notizen-app
  labels:
    app: frontend
spec:
  selector:
    app: frontend
  ports:
    - port: 80
      targetPort: 80
  # NodePort or LoadBalancer for external access
  type: NodePort
