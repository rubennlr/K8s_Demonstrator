apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: notizen-app
spec:
  selector:
    app: postgres
  ports:
    - port: 5432
      targetPort: 5432
  # ClusterIP: only reachable within the cluster
  type: ClusterIP
